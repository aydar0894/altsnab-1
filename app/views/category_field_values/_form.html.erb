<%= form_for(category_field_value) do |f| %>
  <% if category_field_value.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category_field_value.errors.count, "error") %> prohibited this category_field_value from being saved:</h2>

      <ul>
      <% category_field_value.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :item_id %>
    <%= f.text_field :item_id %>
  </div>

  <div class="field">
    <%= f.label :category_field_id %>
    <%= f.text_field :category_field_id %>
  </div>

  <div class="field">
    <%= f.label :value %>
    <%= f.text_field :value %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
