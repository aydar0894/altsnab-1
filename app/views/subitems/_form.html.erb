<%= form_for(subitem) do |f| %>
  <% if subitem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subitem.errors.count, "error") %> prohibited this subitem from being saved:</h2>

      <ul>
      <% subitem.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_area :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :price %>
    <%= f.text_field :price %>
  </div>

  <div class="field">
    <%= f.label :subitem_category_id %>
    <%= f.text_field :subitem_category_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
